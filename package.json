{
  "name": "markdown-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "bootstrap": "^4.4.1",
    "marked": "^0.8.0",
    "react": "^16.5.2",
    "react-dom": "^16.5.2",
    "react-scripts": "^3.4.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "homepage": "https://am-you.github.io/React-markdow-app/",
  "standard": {
    "parser": "babel-eslint",
    "globals": [
      "localStorage"
    ]
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
